html
    head
        meta(charset='utf-8')
        title Oauth project
        link(rel='stylesheet', href='/public/style.css')
    body
        nav
            ul
                li 
                    a(href='/logout') Logout
                li 
                    a(href='/login/auth') Profile
                li 
                    a(href='/logged-in') Homepage

        header 
            h1 Home Page
        main
            p Best OAuth app 
        if alreadyExists 
            hr
            p User already Exists
        if registerSuccess
            hr 
            p User created Succesfully
        if loginFailed 
            hr
            p Login Failed